{
  "name": "cypressproject",
  "version": "1.0.0",
  "description": "sample project - cypress training",
  "main": "index.js",
  "scripts": {
    "e2e:chrome": "cypress run --e2e --browser chrome",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  },
  "dependencies": {
    "@cypress/xpath": "^2.0.3"
  }
}
